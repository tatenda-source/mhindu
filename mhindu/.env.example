# Vercel AI Gateway — required for live Claude vision pest detection.
# Without this set, /api/scout returns a canned demo Detection so the UI flow
# is still testable. Generate via `vercel env pull` once the project is linked
# (see deployment-ops agent docs).
AI_GATEWAY_API_KEY=
